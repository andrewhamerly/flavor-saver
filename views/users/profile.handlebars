<main class="container mx-auto px-4 py-6">
    <!-- User Information Section -->
    <section class="bg-white shadow rounded-lg p-6 mb-6">
      <div class="flex items-center space-x-4">
        <!-- TO DO: May Have To Push Back Profile Picture Feature -->
        <img src="{{user.profileImageUrl}}" alt="{{user.username}}'s profile picture" class="w-16 h-16 rounded-full">
        <div>
          <h2 class="text-xl font-semibold text-gray-900">{{user.username}}</h2>
          <p class="text-gray-600">{{user.bio}}</p>
        </div>
      </div>
    </section>

    <!-- User Recipe List Section -->
    <section>
      <h2 class="text-2xl font-semibold text-gray-900 mb-4">Recipes</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {{#each user.recipes}}
          <div class="bg-white shadow rounded-lg p-4">
            <img src="{{this.imageUrl}}" alt="{{this.title}}" class="w-full h-40 object-cover rounded-t-lg">
            <div class="p-4">
              <h3 class="text-xl font-semibold text-gray-900">{{this.title}}</h3>
              <p class="text-gray-600">{{this.description}}</p>
            </div>
          </div>
        {{/each}}
      </div>
    </section>

    <!-- TO DO: Favorite or Liked List Section  TO DO: Refactor from HTML to Handlebars.js style file. -->
    <section>
    <div class="relative items-center w-full px-5 py-12 mx-auto md:px-12 lg:px-24 max-w-7xl">
        <div class="grid w-full grid-cols-1 gap-6 mx-auto lg:grid-cols-3">
            <div class="p-6">
                <img class="object-cover object-center w-full mb-8 lg:h-48 md:h-36 rounded-xl" src="/assets/images/placeholders/squareCard.png" alt="blog">

                <h1 class="mx-auto mb-8 text-2xl font-semibold leading-none tracking-tighter text-neutral-600 lg:text-3xl">Short headline.</h1>
                <p class="mx-auto text-base leading-relaxed text-gray-500">Free and Premium themes, UI Kit's, templates and landing pages built with Tailwind CSS, HTML &amp; Next.js.</p>

                <div class="mt-4">
                    <a href="#" class="inline-flex items-center mt-4 font-semibold text-blue-600 lg:mb-0 hover:text-neutral-600" title="read more"> Read More » </a>
                </div>
            </div>
            <div class="p-6">
                <img class="object-cover object-center w-full mb-8 lg:h-48 md:h-36 rounded-xl" src="/assets/images/placeholders/squareCard.png" alt="blog">

                <h1 class="mx-auto mb-8 text-2xl font-semibold leading-none tracking-tighter text-neutral-600 lg:text-3xl">Short headline.</h1>
                <p class="mx-auto text-base leading-relaxed text-gray-500">Free and Premium themes, UI Kit's, templates and landing pages built with Tailwind CSS, HTML &amp; Next.js.</p>

                <div class="mt-4">
                    <a href="#" class="inline-flex items-center mt-4 font-semibold text-blue-600 lg:mb-0 hover:text-neutral-600" title="read more"> Read More » </a>
                </div>
            </div>
            <div class="p-6">
                <img class="object-cover object-center w-full mb-8 lg:h-48 md:h-36 rounded-xl" src="/assets/images/placeholders/squareCard.png" alt="blog">

                <h1 class="mx-auto mb-8 text-2xl font-semibold leading-none tracking-tighter text-neutral-600 lg:text-3xl">Short headline.</h1>
                <p class="mx-auto text-base leading-relaxed text-gray-500">Free and Premium themes, UI Kit's, templates and landing pages built with Tailwind CSS, HTML &amp; Next.js.</p>

                <div class="mt-4">
                    <a href="#" class="inline-flex items-center mt-4 font-semibold text-blue-600 lg:mb-0 hover:text-neutral-600" title="read more"> Read More » </a>
                </div>
            </div>
        </div>
    </div>
</section>
  </main>